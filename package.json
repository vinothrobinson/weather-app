{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "This is a JavaScript implementation of a Weather App which utilizes the Weather API to extract weather information.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "push": "git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  }
}
